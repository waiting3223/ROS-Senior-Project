cmake_minimum_required(VERSION 2.8)
project( videocheck )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( videocheck videocheck.cpp )
target_link_libraries( videocheck ${OpenCV_LIBS} )
